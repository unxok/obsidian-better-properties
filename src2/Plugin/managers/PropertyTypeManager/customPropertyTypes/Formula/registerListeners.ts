import { debounce } from "obsidian";
import { CustomPropertyType } from "../../types";
import typeKey from "./type";

export default (({
	plugin,
}) => {}) satisfies CustomPropertyType["registerListeners"];
